# Production Environment Configuration for 46.175.145.84
# Copy this to .env.prod on your production server

# Docker Compose Configuration
COMPOSE_PROJECT_NAME=bot_api

# HTTP/HTTPS Ports
HTTP_PORT=80
HTTPS_PORT=443

# MySQL Configuration
MYSQL_VERSION=8.0
MYSQL_DATABASE=bot_api_db
MYSQL_USER=bot_api_user
MYSQL_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_123
MYSQL_ROOT_PASSWORD=CHANGE_THIS_ROOT_PASSWORD_456

# Redis Configuration
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD_789

# Symfony Backend Configuration
APP_ENV=prod
APP_SECRET=CHANGE_THIS_TO_A_RANDOM_32_CHARACTER_SECRET_KEY_HERE
DATABASE_URL=mysql://bot_api_user:CHANGE_THIS_STRONG_PASSWORD_123@database:3306/bot_api_db?serverVersion=8.0&charset=utf8mb4
REDIS_URL=redis://:CHANGE_THIS_REDIS_PASSWORD_789@redis:6379

# CORS Configuration - Configured for IP 46.175.145.84
# Notice the backslashes before dots - this is required for regex!
CORS_ALLOW_ORIGIN=^https?://(localhost|127\.0\.0\.1|46\.175\.145\.84)(:[0-9]+)?$

# JWT Authentication
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=CHANGE_THIS_JWT_PASSPHRASE

# Telegram Bot (if applicable)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# NestJS Configuration (Optional - for future use)
NESTJS_PORT=3000

# Domain/IP Configuration
DOMAIN=46.175.145.84

# IMPORTANT REMINDERS:
# 1. Change ALL passwords marked with "CHANGE_THIS"
# 2. Generate strong APP_SECRET (32+ characters)
# 3. Use strong JWT_PASSPHRASE
# 4. The CORS_ALLOW_ORIGIN has backslashes before dots - DO NOT remove them!
# 5. After editing, create symlink: ln -sf .env.prod .env

